package spade.test

import prism.enums._
import prism.util._
import prism.test._

import org.scalatest._
import scala.language.reflectiveCalls
import scala.util.Random
import scala.collection.mutable.ListBuffer

class DummyTest extends UnitTest { self =>

  "DummyTest" should "success" taggedAs(WIP) in {
    //new PIR { self =>
      //def main(args: Array[String]) = {}
      //val arch = SN8x8
      //val cus = arch.cuArray
      //// Mapping
      //val router = new ControlBFRouter()

      //val startCU = cus(0)(0)
      //val endCU = cus(3)(3)

      //tic
      //val routes = router.fwdAdvance(
        //start=cus(0)(0),
        //validCons=None,
        //advanceCons=None,
        //end=Some(cus(3)(3)),
        ////reached=None,
        //minHop=1,
        //maxHop=15
      //)
      //toc(s"advance", "ms")
      //println(s"routes:${routes.size}")
    //}
  }

}


